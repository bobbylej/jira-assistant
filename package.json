{
  "name": "doit-jira",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:extension": "cd apps/chrome-extension && node build.js",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "dependencies": {
    "turbo": "^1.10.3"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
